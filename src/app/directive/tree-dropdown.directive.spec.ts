import { TreeDropdownDirective } from './tree-dropdown.directive';

describe('TreeDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
